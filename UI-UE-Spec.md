# SmartAdmin 3.X UI/UE 设计规格说明书

> 基于项目源码逆向梳理，按模块组织的完整 UI/UE 设计规格

---

## 目录

1. [设计系统基础](#1-设计系统基础)
2. [布局系统](#2-布局系统)
3. [登录模块](#3-登录模块)
4. [首页模块](#4-首页模块)
5. [系统管理模块](#5-系统管理模块)
6. [支撑管理模块](#6-支撑管理模块)
7. [业务模块](#7-业务模块)
8. [通用组件库](#8-通用组件库)
9. [交互模式规范](#9-交互模式规范)

---

## 1. 设计系统基础

### 1.1 技术栈

| 类别 | 技术 |
|------|------|
| UI 框架 | Ant Design Vue 4.2.5 |
| 图标库 | @ant-design/icons-vue 7 |
| 图表库 | ECharts 5.4.3 |
| 富文本 | WangEditor 5.6.34 |
| 构建工具 | Vite 5.2.12 |
| CSS 预处理 | Less 4.2 |
| 主题定制 | Ant Design Vue CSS-in-JS Token + Less 变量覆盖 |

### 1.2 色彩系统

**主题色板（10 套可切换）：**

| 序号 | 名称 | 主色 primaryColor | 激活色 activeColor | 悬浮色 hoverColor |
|------|------|-------------------|--------------------|-------------------|
| 0 | 蓝色（默认） | `#1677ff` | `#0958d9` | `#4096ff` |
| 1 | 极客蓝 | `#2F54EB` | `#1d39c4` | `#597ef7` |
| 2 | 翡翠绿 | `#00b96b` | `#00945b` | `#20c77c` |
| 3 | 中国红 | `#F5222D` | `#cf1322` | `#ff4d4f` |
| 4 | 青色 | `#13c2c2` | `#08979c` | `#36cfc9` |
| 5 | 粉色 | `#EB2F96` | `#c41d7f` | `#f759ab` |
| 6 | 紫色 | `#722ED1` | `#531dab` | `#9254de` |
| 7 | 极光绿 | `#52c41a` | `#389e0d` | `#73d13d` |
| 8 | 深绿 | `#009688` | `#007069` | `#1aa391` |
| 9 | 橙色 | `#fa541c` | `#d4380d` | `#ff7a45` |

**功能色：**
- 成功：`#52c41a`
- 警告：`#faad14`
- 错误：`#ff4d4f`
- 信息：`#1677ff`

**全局基础色：**
- 页面背景：`#f8f8f8`
- 基础白：`#fff`
- 悬浮背景：`rgba(0, 0, 0, 0.025)`
- 夜间模式悬浮：`rgba(255, 255, 255, 0.025)`

### 1.3 间距与尺寸

| 变量 | 值 | 用途 |
|------|------|------|
| `@header-height` | 80px | 顶部菜单整体高度（top布局） |
| `@header-user-height` | 40px | 侧边布局头部操作行高度 |
| `@page-tag-height` | 40px | 标签页高度 |
| `@borderRadius` | 0~6px（可配置，默认6） | 全局圆角 |
| `sideMenuWidth` | 默认200px，可配置 | 侧边菜单宽度 |
| `pageWidth` | 默认99%，可配置 | 顶部布局页面宽度 |

### 1.4 排版

| 属性 | 值 |
|------|------|
| 基础字号 | 14px |
| 字体平滑 | `-webkit-font-smoothing: antialiased` |
| 行高 | 1.5（Ant Design 默认） |

### 1.5 主题配置项一览

配置通过 Pinia Store（`appConfigStore`）管理，持久化至 `localStorage`：

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `language` | string | `'zh_CN'` | 国际化语言 |
| `layout` | string | `'side'` | 布局模式 |
| `sideMenuWidth` | number | 200 | 侧边菜单宽度(px) |
| `pageTagLocation` | string | `'center'` | 标签页位置(top/center) |
| `darkModeFlag` | boolean | false | 夜间模式 |
| `sideMenuTheme` | string | `'dark'` | 菜单主题(dark/light) |
| `colorIndex` | number | 0 | 主题色索引 |
| `pageWidth` | string | `'99%'` | 顶部布局页面宽度 |
| `borderRadius` | number | 6 | 圆角大小 |
| `menuSingleExpandFlag` | boolean | true | 菜单手风琴模式 |
| `pageTagFlag` | boolean | true | 显示标签页 |
| `pageTagStyle` | string | `'chrome'` | 标签页风格 |
| `breadCrumbFlag` | boolean | true | 显示面包屑 |
| `footerFlag` | boolean | true | 显示页脚 |
| `helpDocFlag` | boolean | true | 显示帮助文档 |
| `helpDocExpandFlag` | boolean | false | 帮助文档默认展开 |
| `watermarkFlag` | boolean | true | 显示水印 |
| `websiteName` | string | `'SmartAdmin 3.X'` | 网站名称 |
| `primaryColor` | string | `'#1677ff'` | 主色 |
| `compactFlag` | boolean | false | 紧凑模式 |

---

## 2. 布局系统

### 2.1 四种布局模式

#### 模式一：Side（传统侧边栏）

```
┌──────────────────────────────────────────────────┐
│                    Header (40px)                  │
│ [≡] [🏠] [面包屑/标签页(top)]        [消息][⚙][👤] │
├─────────┬────────────────────────────────────────┤
│         │           标签页(center) 40px           │
│  Side   ├────────────────────────────────────────┤
│  Menu   │                                        │
│ (200px) │           Content 内容区                │
│  dark/  │                                        │
│  light  │                                        │
│         ├────────────────────────────────────────┤
│         │              Footer 40px               │
└─────────┴────────────────────────────────────────┘
```

- 侧边菜单固定宽度（默认200px，可配置），支持折叠
- 头部行高度 40px，左侧：折叠按钮 + 首页按钮 + 面包屑/标签页
- 标签页位置可选 top（嵌入头部行内）或 center（头部下方独立行）
- 内容区高度 = 100% - header - tagNav - footer

#### 模式二：Side-Expand（展开侧边栏）

```
┌──────────────────────────────────────────────────┐
│                    Header (40px)                  │
│ [≡] [🏠] [面包屑/标签页(top)]        [消息][⚙][👤] │
├──────┬───────────────────────────────────────────┤
│      │            标签页(center) 40px             │
│ Side ├───────────────────────────────────────────┤
│Expand│                                           │
│ Menu │            Content 内容区                  │
│(auto)│                                           │
│      ├───────────────────────────────────────────┤
│      │              Footer 40px                  │
└──────┴───────────────────────────────────────────┘
```

- 与 Side 布局结构一致，差异在侧边菜单：
  - 侧边菜单宽度 `auto`，自适应内容宽度
  - 顶部显示一级菜单（横向），下方显示选中一级菜单的子菜单（纵向）
  - 折叠时仅显示一级菜单图标

#### 模式三：Top（顶部菜单）

```
┌──────────────────────────────────────────────────┐
│              Top Menu (48px, fixed)               │
│ [Logo] [菜单项1] [菜单项2] ...      [消息][⚙][👤]  │
├──────────────────────────────────────────────────┤
│  标签页 (fixed, 40px)                             │
├──────────────────────────────────────────────────┤
│  面包屑 (fixed, 40px)                             │
├──────────────────────────────────────────────────┤
│                                                  │
│              Content 内容区                        │
│            (宽度可配置，居中)                        │
│                                                  │
├──────────────────────────────────────────────────┤
│                   Footer 40px                    │
└──────────────────────────────────────────────────┘
```

- 顶部菜单栏固定定位，高度 48px
- 标签页和面包屑独立于内容区，固定在顶部菜单下方
- 内容区宽度可配置（百分比或像素），水平居中
- 无侧边栏

#### 模式四：Top-Expand（分组顶部菜单）

- 与 Top 布局结构相似
- 差异：顶部菜单使用分组展开样式，一级菜单点击后展开显示二级菜单

### 2.2 布局公共组件

所有布局共享以下子组件：

| 组件 | 位置 | 说明 |
|------|------|------|
| `PageTag` | Header 内或独立行 | 页面标签页导航 |
| `MenuLocationBreadcrumb` | Header 内 | 面包屑导航 |
| `HeaderUserSpace` | Header 右侧 | 消息通知、设置、用户头像 |
| `SmartFooter` | 底部 | 版权信息 |
| `SideHelpDoc` | 右侧侧边栏(180px) | 帮助文档 |
| `RegularChangePasswordModal` | 全局 | 定期修改密码弹窗 |
| `IframeIndex` | Content 内 | iframe 嵌入页 |
| `BackTop` | 右下角 | 回到顶部，触发距离80px |

### 2.3 Keep-Alive 策略

- 非 iframe 页面：使用 `<keep-alive :include="keepAliveIncludes">`，最多缓存 30 个组件
- iframe 页面：每个 iframe 一个独立组件实例，通过 `v-show` 切换显示
- 缓存组件名 = 菜单的 `menuId.toString()`
- 菜单配置 `cacheFlag` 控制是否缓存

### 2.4 水印

- 内容区可选显示用户姓名水印
- 基于 Canvas 实现，挂载到 `#smartAdminLayoutContent`
- 全屏模式下不显示菜单和头部，但内容区和水印仍可见

---

## 3. 登录模块

### 3.1 风格一：深色左栏 + 白色右栏

```
┌──────────────────────────────────────┐
│         全屏背景图 (login-bg.png)       │
│  ┌─────────────┬──────────────┐      │
│  │   深色区域    │   白色区域    │      │
│  │  #003b94     │  #ffffff     │      │
│  │             │              │      │
│  │ 欢迎登录     │  账号登录     │      │
│  │ SmartAdmin  │  [用户名]     │      │
│  │ V3          │  [密  码]     │      │
│  │             │  [验证码] 🖼  │      │
│  │ 介绍文字     │  ☑ 记住密码   │      │
│  │ ...         │  [登 录]     │      │
│  │             │              │      │
│  │ [二维码1]    │ ── 其他方式 ──│      │
│  │ [二维码2]    │ 🟢🔵🔴🟡... │      │
│  └─────────────┴──────────────┘      │
└──────────────────────────────────────┘
```

**设计参数：**
- 容器：Flex 居中，宽 888px(444×2)，高 600px
- 左栏：深蓝 `#003b94`，圆角 左12px 右0，padding 23px 25px
- 右栏：白色，圆角 右12px 左0，padding 34px 42px，1px `#efefef` 边框
- 标题：30px 700 居中
- 输入框：44px 高，1px `#ededed` 边框，4px 圆角
- 登录按钮：350px × 50px，背景 `#1748FD`，16px 700 白色
- 验证码：输入框 60% 宽 + 图片
- 右上角：QR 角标 66×66px
- 分割线：114px `#e6e6e6`
- 第三方图标：30×30px

### 3.2 风格二：白色左栏(内嵌图片) + 白色右栏

```
┌──────────────────────────────────────┐
│         全屏背景图 (login-bg.png)       │
│  ┌─────────────┬──────────────┐      │
│  │   白色区域    │   白色区域    │      │
│  │  #ffffff     │  #ffffff     │      │
│  │ ┌───────────┐│              │      │
│  │ │ 内嵌背景图  ││  账号登录     │      │
│  │ │ left-bg1  ││  [用户名]     │      │
│  │ │ 欢迎文字   ││  [密  码]     │      │
│  │ │ 居中显示   ││  [验证码] 🖼  │      │
│  │ └───────────┘│  [登 录]     │      │
│  └─────────────┴──────────────┘      │
└──────────────────────────────────────┘
```

**与风格一差异：**
- 左栏背景 `#ffffff`，内部嵌套背景图 `left-bg1.png`，8px 圆角，带阴影
- 左栏文字为深色 `#333333`
- 欢迎标题 26px 700，副标题 20px 700
- 右栏登录标题 20px（非30px）
- 无 `#efefef` 边框

### 3.3 风格三：白色左栏(GIF动图) + 白色右栏

```
┌──────────────────────────────────────┐
│         全屏背景图 (login-bg.png)       │
│  ┌─────────────┬──────────────┐      │
│  │   白色区域    │   白色区域    │      │
│  │  #ffffff     │  #ffffff     │      │
│  │  欢迎登录     │              │      │
│  │ ┌──────────┐│  账号登录     │      │
│  │ │ GIF动图   ││  [用户名]     │      │
│  │ │ login.gif ││  [密  码]     │      │
│  │ │ 350x350  ││  [验证码] 🖼  │      │
│  │ └──────────┘│  [登 录]     │      │
│  │  蓝色胶囊按钮 │              │      │
│  └─────────────┴──────────────┘      │
└──────────────────────────────────────┘
```

**与风格一/二差异：**
- 容器宽度 460px×2（比前两者宽16px）
- 左栏纯白背景，居中显示 GIF 动图（`login.gif`，350×350px）
- 副标题为蓝色胶囊按钮：`#1748fd` 背景，22px 圆角
- 整体更现代、活泼

### 3.4 登录交互流程

```
加载登录页
  ├─ 获取验证码 → 显示 base64 图片 → 定时刷新(过期前5秒)
  ├─ 获取双因子标识 → 显示/隐藏邮箱验证码输入
  │
  ├─ 输入用户名(admin) + 密码 + 验证码
  │   ├─ 回车键提交
  │   ├─ 密码加密(encryptData) → 调用登录接口
  │   │   ├─ 成功 → 存token → setUserLoginInfo → 初始化字典 → buildRoutes → 跳转/home
  │   │   └─ 失败 → 清空验证码 → 刷新验证码 → 显示错误
  │   └─ 邮箱验证码(可选) → 60秒倒计时
  │
  └─ 记住密码 → 本地存储
```

### 3.5 第三方登录展示

7 种第三方登录图标（装饰性，非功能实现）：
微信、支付宝、抖音、QQ、微博、飞书、Google

---

## 4. 首页模块

### 4.1 整体布局

```
┌──────────────────────────────────────────────────┐
│              HomeHeader (a-card)                  │
│  欢迎语，{用户名}   |   所属部门：{部门}   |  日期信息  │
│  上次登录: {时间}; 设备: {浏览器} {OS}; {地区}; {IP}  │
│  ☺ 毒鸡汤/励志语句                    [天气预报iframe] │
├───────────────────────────────┬──────────────────┤
│  左侧 (span=16)               │  右侧 (span=8)    │
│  ┌──────────┬──────────┐     │  ┌──────────────┐│
│  │ 公告(span12)│ 通知(span12)│     │  │ 公众号卡片    ││
│  ├──────────┴──────────┤     │  ├──────────────┤│
│  │ 饼图(span12) │ 柱图(span12)│     │  │ 更新日志卡片  ││
│  ├──────────────────────┤     │  ├──────────────┤│
│  │ 渐变面积图(span24)     │     │  │ 待办卡片      ││
│  └──────────────────────┘     │  └──────────────┘│
└───────────────────────────────┴──────────────────┘
```

栅格间距：`[10, 10]`

### 4.2 子模块设计

#### HomeHeader
- 容器：`a-card`
- 欢迎语：根据时段显示「午夜好/早上好/中午好/下午好/晚上好，{用户姓名}」
- 副标题：所属部门
- 附加信息：阳历日期 + 星期 + 农历 + 节气
- 上次登录：时间 + 设备(UA解析) + 地区 + IP
- 励志语句：从数组随机选取
- 天气：嵌入第三方天气 iframe

#### HomeNotice（公告/通知）
- 容器：`DefaultHomeCard`，标题 + 图标 + 「更多」
- 列表：6条，未读红点/已读灰点
- 空状态：`a-empty`
- 加载：`a-spin`

#### ECharts 图表
- `Pie`：饼图
- `Category`：分类柱状图
- `Gradient`：渐变面积图

#### 右侧卡片
- `OfficialAccountCard`：公众号二维码
- `ChangelogCard`：更新日志列表
- `ToBeDoneCard`：待办事项列表

### 4.3 DefaultHomeCard 通用卡片

- 顶部：图标 + 标题 + 「更多」extra 链接
- 内容区：slot 默认插槽
- 规格自适应内容高度

---

## 5. 系统管理模块

### 5.1 员工管理

#### 布局：左右分栏

```
┌──────────────┬──────────────────────────────────┐
│  部门树 (240px) │  员工列表                          │
│              │  [查询条件行]                        │
│  🔍 搜索部门   │  部门/姓名/手机/状态 → 查询 重置       │
│  ├ 总公司     │                                  │
│  │├ 研发部    │  [新增员工] [修改密码]                │
│  │├ 市场部    │  ┌──────────────────────────────┐│
│  │└ 财务部    │  │ 表格：姓名/登录账号/手机/部门/    ││
│  └ 行政部     │  │       状态/创建时间/操作         ││
│              │  │ 操作：编辑/修改密码/禁用          ││
│              │  └──────────────────────────────┘│
│              │  分页                             │
└──────────────┴──────────────────────────────────┘
```

- 左侧：`a-tree` 组件，宽度 240px，支持搜索、选中联动
- 点击部门节点 → 过滤右侧员工列表
- 右侧：查询表单 + 操作按钮 + 表格 + 分页

#### 员工表单弹窗（Modal）
- 字段：姓名、登录账号、手机号、部门（树选择）、岗位（下拉）、状态（开关）
- 支持 新增/编辑 两种模式

### 5.2 菜单管理

#### 布局：单列表格

```
┌─────────────────────────────────────────────────┐
│  [新增菜单]                                       │
│  ┌───────────────────────────────────────────┐  │
│  │ 树形表格：名称/类型/图标/路径/组件/权限/排序/操作│  │
│  │  ├ 系统管理                                 │  │
│  │  │ ├ 员工管理   [编辑][删除]                  │  │
│  │  │ ├ 菜单管理   [编辑][删除]                  │  │
│  │  │ └ 角色管理   [编辑][删除]                  │  │
│  └ 支撑管理                                    │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

- 表格使用 `children` 展示树形层级
- 类型列：目录(1)/菜单(2)/功能点(3)

#### 菜单操作弹窗（Modal）
- 字段：菜单类型(单选)、上级菜单(树选择)、菜单名称、图标(图标选择器)、路径、组件、排序、权限标识、可见(开关)、缓存(开关)、外链(开关)

### 5.3 角色管理

#### 布局：左右分栏

```
┌─────────────────┬─────────────────────────────┐
│  角色列表 (320px)  │  角色详情/权限配置               │
│                 │                             │
│  [新增角色]       │  ┌───────────────────────┐ │
│  ┌─────────────┐│  │  角色设置 Tab            │ │
│  │ 角色名/描述   ││  │  ┌───────┬───────┐     │ │
│  │ ● 管理员     ││  │  │功能权限│数据范围│     │ │
│  │ ○ 普通用户   ││  │  ├───────┴───────┤     │ │
│  │ ○ 审计员     ││  │  │ 权限树(复选框)    │     │ │
│  └─────────────┘│  │  │ ☑ 系统管理       │     │ │
│                 │  │  │  ☑ 员工管理      │     │ │
│                 │  │  │  ☐ 菜单管理      │     │ │
│                 │  │  └──────────────────┘ │ │
│                 │  └───────────────────────┘ │
└─────────────────┴─────────────────────────────┘
```

- 左侧：角色列表卡片，选中高亮
- 右侧：`a-tabs` 切换「功能权限」和「数据范围」
- 功能权限：权限树（`a-tree` + checkbox），支持半选
- 数据范围：自定义视图

#### 角色表单弹窗（Modal）
- 字段：角色名称、角色描述

### 5.4 部门管理

#### 布局：树形表格

```
┌─────────────────────────────────────────────────┐
│  [新增部门]                                       │
│  ┌───────────────────────────────────────────┐  │
│  │ 表格：部门名称/负责人/排序/创建时间/操作       │  │
│  │  ├ 总公司            [新增子部门][编辑][删除]  │  │
│  │  │├ 研发部           [新增子部门][编辑][删除]  │  │
│  │  │└ 市场部           [新增子部门][编辑][删除]  │  │
│  └ 行政部              [新增子部门][编辑][删除]  │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

#### 部门表单弹窗（Modal）
- 字段：部门名称、负责人、上级部门(树选择)、排序

### 5.5 岗位管理

#### 布局：单列表格
- 查询：岗位名称/状态
- 表格列：岗位名称/排序/状态/创建时间/操作
- 表单弹窗：岗位名称、排序、状态(开关)

### 5.6 个人中心

#### 布局：左侧Tab菜单 + 右侧内容

```
┌─────────────────────────────────────────────────┐
│  ┌──────────┬──────────────────────────────┐   │
│  │  左侧菜单  │         右侧内容              │   │
│  │          │                              │   │
│  │ ● 基本信息 │  (根据选中Tab显示对应内容)       │   │
│  │ ○ 修改密码 │                              │   │
│  │ ○ 消息    │                              │   │
│  │ ○ 通知    │                              │   │
│  │ ○ 操作日志 │                              │   │
│  └──────────┴──────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

---

## 6. 支撑管理模块

### 6.1 字典管理

#### 布局：主表 + 弹窗详情表

```
┌─────────────────────────────────────────────────┐
│  查询：编码/名称/状态 → 查询 重置                     │
│  [新增]                                           │
│  ┌───────────────────────────────────────────┐  │
│  │ 编码/名称/状态/创建时间/操作                    │  │
│  │  xxx    xxx   启用   2026-01-01  [字典值][编辑]│  │
│  └───────────────────────────────────────────┘  │
│  分页                                             │
└─────────────────────────────────────────────────┘

点击[字典值] → 弹窗展示该字典下的数据项列表：
┌───────────────────────────────────────┐
│  字典数据 - {字典名称}                   │
│  [新增]                                 │
│  ┌───────────────────────────────────┐│
│  │ 值/名称/排序/状态/操作              ││
│  │  1  男   1  启用  [编辑][禁用]      ││
│  │  2  女   2  启用  [编辑][禁用]      ││
│  └───────────────────────────────────┘│
└───────────────────────────────────────┘
```

- 二级弹窗嵌套：字典表单弹窗 → 字典数据弹窗 → 字典数据表单弹窗

### 6.2 系统配置

- 查询：参数Key
- 表格：Key/Value/备注/创建时间/操作
- 表单弹窗：Key、Value(文本域)、备注

### 6.3 文件管理

- 查询：文件类型/文件名
- 表格：类型(图标)/文件名/大小/创建人/创建时间/操作
- 操作：上传、预览、下载
- 文件预览：支持图片预览（`v-viewer`）

### 6.4 操作日志

- 查询：用户名/模块/操作时间范围
- 表格：操作人/模块/内容/时间/结果(成功/失败)/IP
- 详情弹窗：展示完整操作信息

### 6.5 登录日志

- 查询：用户名/IP/登录时间范围
- 表格：用户名/IP/设备信息/地区/时间/结果

### 6.6 更新日志

- 查询：版本号/更新类型
- 表格：版本号/更新类型/发布人/发布时间/操作
- 表单弹窗：版本号、更新类型(下拉)、发布日期、更新内容(富文本)

### 6.7 单号管理

- 表格：业务名称/格式/循环周期/操作
- 操作：生成单号（弹窗表单）、查看记录

### 6.8 缓存管理

- 表格：缓存Key/Value
- 操作：删除缓存

### 6.9 定时任务

- 表格：任务名称/执行类/Cron表达式/状态/操作
- 操作：新增(弹窗)、编辑、启动/暂停、执行记录(弹窗)

### 6.10 代码生成器

- 表格：表名/表注释/引擎/创建时间/操作
- 操作：配置（弹窗多步表单：基本信息→字段配置→查询配置→新增修改配置→删除配置）、预览代码（弹窗带语法高亮）

### 6.11 等保配置

- 两个 Tab：数据脱敏列表 + 等保配置
- 数据脱敏：规则名称/脱敏方式/操作

### 6.12 API 加密

- 展示 RSA 密钥对信息

### 6.13 热加载

- 表格：类名/操作
- 操作：执行热加载（确认弹窗）

### 6.14 消息管理

- 表格：标题/类型/发送人/时间/已读/操作
- 发送消息表单：接收人(员工选择器)、标题、内容

---

## 7. 业务模块

### 7.1 OA - 企业管理

#### 企业列表

```
┌─────────────────────────────────────────────────┐
│  查询：企业名称/类型/状态 → 查询 重置                  │
│  [新增企业]                                       │
│  ┌───────────────────────────────────────────┐  │
│  │ 企业名称/类型/联系人/电话/状态/操作            │  │
│  │  xxx  有限  张三  138...  启用  [详情][编辑] │  │
│  └───────────────────────────────────────────┘  │
│  分页                                             │
└─────────────────────────────────────────────────┘
```

#### 企业详情

```
┌─────────────────────────────────────────────────┐
│  [返回列表]  {企业名称}                             │
│  ┌───────────────────────────────────────────┐  │
│  │  基本信息 / 企业银行 / 企业员工 / 企业发票      │  │
│  ├───────────────────────────────────────────┤  │
│  │  (Tab 内容)                                │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

- 4 个 Tab 页：基本信息、银行列表、员工列表、发票列表
- 每个 Tab 内可独立新增/编辑/删除

### 7.2 OA - 通知公告

#### 管理端（notice-list）
- 表格：标题/类型/发布人/发布时间/可见范围/状态/操作
- 新增/编辑：抽屉式表单（Drawer）
  - 基本信息：标题、类型、内容(富文本)
  - 可见范围：按部门/按员工（Transfer 穿梭框）
- 查看详情：弹窗展示

#### 员工端（notice-employee-list）
- 表格：标题/类型/发布人/发布时间/已读状态
- 查看详情跳转

### 7.3 ERP - 商品分类

#### 树形表格

```
┌─────────────────────────────────────────────────┐
│  [新增分类]                                       │
│  ┌───────────────────────────────────────────┐  │
│  │ 分类名称/排序/状态/创建时间/操作               │  │
│  │  ├ 电子产品         [新增子分类][编辑][删除]   │  │
│  │  │├ 手机            [新增子分类][编辑][删除]   │  │
│  │  │└ 电脑            [新增子分类][编辑][删除]   │  │
│  └ 服装               [新增子分类][编辑][删除]   │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

### 7.4 ERP - 商品列表

- 查询：商品名称/分类(树选择)
- 表格：商品名称/分类/价格/状态/操作
- 新增/编辑：弹窗表单

---

## 8. 通用组件库

### 8.1 框架组件（framework）

| 组件 | 用途 | 交互方式 |
|------|------|----------|
| `BooleanSelect` | 布尔值选择 | 下拉：是/否 |
| `SmartEnumSelect` | 枚举选择器 | 下拉，从枚举定义生成选项 |
| `SmartEnumRadio` | 枚举单选 | Radio 组，从枚举定义生成 |
| `SmartEnumCheckbox` | 枚举多选 | Checkbox 组 |
| `SmartCopyIcon` | 复制图标 | 点击复制到剪贴板，tooltip 反馈 |
| `SmartLoading` | 全局加载 | `SmartLoading.show()` / `SmartLoading.hide()` |
| `IconSelect` | 图标选择器 | 弹窗网格，展示 Ant Design 图标集 |
| `AreaCascader` | 地区级联 | 三级级联：省/市/区 |
| `TextEllipsis` | 文本省略 | 超长文本截断 + tooltip 显示全文 |
| `Wangeditor` | 富文本编辑器 | WangEditor 集成，支持图片上传 |
| `IframeIndex` | iframe 嵌入页 | 外链菜单以 iframe 形式嵌入 |

### 8.2 支撑组件（support）

| 组件 | 用途 | 交互方式 |
|------|------|----------|
| `DictSelect` | 字典下拉选择 | 传入 dictCode，自动加载字典选项 |
| `DictCodeSelect` | 字典编码选择 | 传入 dictCode，显示编码值 |
| `DictLabel` | 字典标签展示 | 将字典值转为可视化标签（带颜色） |
| `FileUpload` | 文件上传 | 拖拽/点击上传，支持多文件，文件类型限制 |
| `FilePreview` | 文件预览 | 图片内联预览，文件链接下载 |
| `FilePreviewModal` | 文件预览弹窗 | 弹窗内大图预览 |
| `TableOperator` | 表格操作栏 | 行内操作按钮组（编辑/删除等） |
| `TableHeaderCell` | 表格头单元格 | 自定义表头（含排序、筛选） |
| `DataTracer` | 数据追踪 | 展示数据创建/修改记录（时间轴 + 表格） |
| `SmartTableColumnModal` | 表格列配置 | 弹窗配置显示/隐藏列、列顺序 |

### 8.3 业务组件（business）

| 组件 | 用途 |
|------|------|
| `CategoryTreeSelect` | 分类树选择器（ERP商品分类） |
| `EnterpriseSelect` | 企业选择下拉 |
| `EnterpriseBankSelect` | 企业银行选择 |
| `EnterpriseInvoiceSelect` | 企业发票选择 |

### 8.4 系统组件（system）

| 组件 | 用途 |
|------|------|
| `DepartmentTreeSelect` | 部门树选择器 |
| `EmployeeSelect` | 员工选择下拉 |
| `EmployeeTableSelectModal` | 员工表格选择弹窗（支持搜索和批量选择） |
| `MenuTreeSelect` | 菜单树选择器 |
| `PositionSelect` | 岗位选择下拉 |

---

## 9. 交互模式规范

### 9.1 标签页导航

**三种风格：**

| 风格 | 标识 | 视觉 |
|------|------|------|
| 默认 | `default` | 简洁下划线标签 |
| Ant Design | `antd` | Ant Design 风格 Tab |
| Chrome | `chrome` | 模拟浏览器标签页，带圆角梯形 |

**行为规则：**
- 首页标签固定，不可关闭
- 其他标签右键/点击可关闭
- 标签数据持久化至 localStorage
- 标签数量不限，水平滚动
- 切换标签时保留页面状态（keep-alive）

### 9.2 表单交互

| 模式 | 适用场景 | 组件 |
|------|----------|------|
| Modal 弹窗 | 新增/编辑简单表单 | `a-modal` + `a-form` |
| Drawer 抽屉 | 复杂表单、需要更大空间 | `a-drawer` |
| 页面内嵌 | 列表内搜索表单 | `a-form` inline |
| 多步表单 | 复杂配置（如代码生成器） | `a-steps` + 多个表单 |

**表单布局规范：**
- 弹窗表单：水平布局 `label-col: { span: 6 }`, `wrapper-col: { span: 18 }`
- 搜索表单：行内布局 `a-form layout="inline"`，使用 `.smart-query-form-row` 分行
- 字段间距：`margin-right: 8px`, `margin-top: 5px`, `margin-bottom: 5px`

### 9.3 表格交互

**标准表格结构：**
```
┌───────────────────────────────────────────────┐
│  [查询条件行] (smart-query-form-row)            │
│  字段1 字段2 字段3 → [查询] [重置]               │
├───────────────────────────────────────────────┤
│  [操作按钮区] (smart-table-btn-block)           │
│  [新增] [批量删除]              [列设置⚙]        │
├───────────────────────────────────────────────┤
│  a-table                                       │
│  斑马纹：奇数行 background: #fafafa             │
├───────────────────────────────────────────────┤
│  分页 (smart-query-table-page)                  │
│  右对齐，显示总条数                               │
└───────────────────────────────────────────────┘
```

**操作列：**
- 按钮组：编辑、删除、查看等
- 使用 `smart-table-operate` 类，按钮紧凑（padding: 0 3px）
- 危险操作（删除/禁用）：二次确认 `Modal.confirm`

### 9.4 面包屑导航

- 显示当前页面在菜单树中的层级路径
- 格式：`一级菜单 > 二级菜单 > 当前页面`
- 可配置显示/隐藏

### 9.5 设置面板

- 入口：头部右侧齿轮图标
- 形式：右侧抽屉（Drawer），宽度 450px
- 配置项实时生效，自动保存至 localStorage
- 底部固定：「复制配置信息」+「恢复默认配置」
- 复制配置：生成 JSON，提示修改 `app-config.js`

### 9.6 全局反馈

| 类型 | 组件 | 场景 |
|------|------|------|
| 成功消息 | `message.success` | 操作成功 |
| 错误消息 | `message.error` | 操作失败、接口异常 |
| 加载中 | `SmartLoading` / `a-spin` | 接口请求等待 |
| 确认弹窗 | `Modal.confirm` | 删除、禁用等危险操作 |
| 信息弹窗 | `Modal.info` | 提示信息 |
| 通知 | `notification` | 系统提示（如登录页温馨提示） |

### 9.7 权限控制

- 指令：`v-privilege`，控制按钮/元素的显示隐藏
- 菜单权限：通过路由守卫动态构建路由，无权限的菜单不显示
- 功能点权限：后端返回 `pointsList`，前端根据权限点控制操作按钮

### 9.8 国际化

- 支持语言：中文（zh_CN）、英文（en-US）
- 切换入口：设置面板
- 实现方式：vue-i18n + `$t()` 函数

### 9.9 响应式

- 侧边菜单支持折叠（collapsed）
- 窗口 resize 事件监听，自动调整内容区高度
- 顶部布局标签页和面包屑固定定位
- 内容区最小高度自适应

### 9.10 路由与页面缓存

| 行为 | 规则 |
|------|------|
| 页面进入 | 菜单 `cacheFlag=true` → 自动加入 keep-alive |
| 标签关闭 | 从 keep-alive 中移除 |
| 最大缓存数 | 30 个 |
| 刷新页面 | 从 localStorage 恢复标签状态和缓存列表 |
| iframe 页面 | 每个独立实例，`v-show` 切换 |

---

> **文档版本**：v1.0 | **生成日期**：2026-07-02 | **基于**：SmartAdmin 3.0.0 (JavaScript)
