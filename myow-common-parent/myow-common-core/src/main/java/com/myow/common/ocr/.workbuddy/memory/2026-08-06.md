# 2026-08-06 工作日志

## DO 解析模块 · 收尾与接口化

### 1. 鲁棒性加固收尾（上午/延续前序）
- 确认定稿阈值：MAX_LABEL_ROW_OVERLAP=0.35f、SIDE_BY_SIDE_TOLERANCE=1.0f、MAX_SPLIT_DEPTH=3、DOMINANT_GAP_RATIO=2.2f。
- 全量回归通过：基线 21/21 精确匹配；场景3(拟真OCR噪声) ±0.10°/±0.25° 100%、±0.50° 98%、±0.75° 95%、±1.00° 88%；场景4(独立抖动) ±1.5pt 90%、±2.0pt 67%、±2.5pt 28%。
- 复核基线 4 个 null（free_time_exp / container.seal_no / weight / quantity）为表单真实空白，非漏抽。
- 清理：删除临时 CellDump.java；JitterFieldDiag 去反射、直接调 parseRealisticNoise（已提为 public）。
- 残留短板：±1.00° 下的失败全部集中在 cargo 区块（description 渗值 / total_packages 丢值）。

### 2. 新增 HTTP 接口（文件入参）
- 新增 `src/main/java/com/myow/common/ocr/api/DeliveryOrderController.java`
  - `POST /api/delivery-orders/parse`：multipart `file` + 可选 `dpi`(默认300)，自动识别 PDF(文本层优先) / 图片(OCR通道)，返回 ParseResult(JSON，含 21 字段 + 元信息)。
  - `GET /api/delivery-orders/health`：存活探针。
  - 错误用统一 `ApiError{code,detail}`，解析失败返回 422。
- pom 新增 `spring-web`(provided 作用域)，由 Web 应用运行时提供；compile 通过。
- OCR 通道仍依赖本机 Tesseract + tessdata（当前未装，故真实扫描件通道未实跑）。
