# DEV Environment Configuration

# Database Configuration (PostgreSQL)
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    jdbc-url: jdbc:postgresql://localhost:5432/myow_oms_dev?stringtype=unspecified&useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: myow_admin
    password: MyowPass2026!
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 5
      pool-name: MyOW-HikariCP

# Redis Configuration
spring.data.redis:
  host: localhost
  port: 6379
  password: MyowRedis2026!
  database: 0
  lettuce:
    pool:
      max-active: 8
      max-wait: -1ms
      max-idle: 8
      min-idle: 0
